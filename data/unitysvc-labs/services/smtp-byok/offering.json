{
  "name": "smtp-byok",
  "schema": "offering_v1",
  "service_type": "email",
  "status": "ready",
  "time_created": "2026-03-27T00:00:00Z",
  "description": "Bring Your Own Key SMTP relay. Connect your own SMTP server (Gmail, SendGrid, Mailgun, SES, etc.) and send emails through the UnitySVC gateway. Customers provide their own SMTP credentials.",
  "tags": [
    "email",
    "smtp",
    "byok"
  ],
  "capabilities": [
    "smtp_relay"
  ],
  "payout_price": {
    "description": "No platform commission — customer uses own SMTP",
    "percentage": "100.00",
    "type": "revenue_share"
  },
  "upstream_access_config": {
    "Customer SMTP": {
      "access_method": "smtp",
      "host": "${ customer_secrets.SMTP_HOST }",
      "port": "${ customer_secrets.SMTP_PORT }",
      "username": "${ customer_secrets.SMTP_USERNAME }",
      "password": "${ customer_secrets.SMTP_PASSWORD }",
      "tls": true
    }
  }
}
