{
  "capabilities": [
    "smtp_relay"
  ],
  "description": "Multi-enrollment SMTP relay. Each enrollment connects to a different SMTP server. Customers provide secret names as parameters, enabling multiple relay configurations (e.g., personal Gmail, work SendGrid, transactional SES) under a single account.",
  "name": "smtp-relay-multi",
  "payout_price": {
    "description": "No platform commission \u2014 customer uses own SMTP",
    "percentage": "100.00",
    "type": "revenue_share"
  },
  "schema": "offering_v1",
  "service_type": "email",
  "status": "ready",
  "tags": [
    "email",
    "smtp",
    "byok"
  ],
  "time_created": "2026-03-27T00:00:00Z",
  "upstream_access_config": {
    "Customer SMTP": {
      "access_method": "smtp",
      "host": "${ customer_secrets.{{ params.SMTP_HOST_SECRET }} }",
      "password": "${ customer_secrets.{{ params.SMTP_PASSWORD_SECRET }} }",
      "port": "${ customer_secrets.{{ params.SMTP_PORT_SECRET }} }",
      "tls": true,
      "username": "${ customer_secrets.{{ params.SMTP_USERNAME_SECRET }} }"
    }
  }
}
